# Minimal requirements for vLLM testing
# Use this if the full requirements.txt has conflicts
# Install with: pip install -r requirements-minimal.txt

# Core vLLM dependencies
vllm==0.5.4
torch==2.4.0
transformers>=4.43.2
tokenizers>=0.19.1
safetensors>=0.4.1
huggingface-hub==0.36.0  # Compatible with transformers 4.57.1
accelerate>=0.25.0

# Version constraints to prevent conflicts
packaging==23.2
urllib3==1.26.18
click==8.1.7

# vLLM Additional Requirements (often missing)
outlines==0.0.46  # Specific version that works
pyairports  # Let pip resolve version
jsonschema>=4.0.0

# Essential utilities
numpy>=1.24.3
sentencepiece==0.1.99
protobuf>=4.25.1
einops==0.7.0

# API essentials (if needed)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Basic monitoring
psutil==5.9.6
tqdm==4.66.1